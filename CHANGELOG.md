# Changelog

כל שינוי משמעותי לפרויקט מתועד כאן.
פורמט: [Keep a Changelog](https://keepachangelog.com) · גרסאות: [SemVer](https://semver.org).

## [1.21.0] — 2026-09-15

### Added (נוסף)
- **תיבת סימון "טיוטה להגשה"** בסרגל הכלים — כשמסומנת, מוסיפה סימן מים
  אלכסוני עדין לכל עמודי "דוח סקירה" המיוצא (גם בקובץ הבודד וגם בתוך
  ה-ZIP), ומקדימה "טיוטה - " לשם הקובץ. הטקסט נוסף כשכבת HTML/CSS על
  העמוד לפני הצילום (לא כטקסט וקטורי ב-jsPDF, שלא תומך בעברית בגופנים
  המובנים) כדי שיירונדר נכון. לא נשמר ב-state/localStorage — משפיע רק על
  הקובץ שמיוצא באותו רגע — [index.html](index.html), [app.js](app.js), [pdf.js](pdf.js), [style.css](style.css)

### Changed (שונה)
- **הוסר תוכן אוטומטי ממקטע "הערות הסוקר" בדוח** — בעבר כלל פרשנות
  אוטומטית של ציוני CPI Av/Crit וסייג אחריות קבוע, לצד ההערות שהוזנו
  בפועל. לפי בקשה מפורשת, המקטע מציג כעת רק הערות שהוזנו בלשונית "הערות
  הסוקר"; אם ריקה — מוצג "לא הוזנו הערות סוקר" בלבד — [pdf.js](pdf.js)

## [1.20.0] — 2026-09-15

### Added (נוסף)
- **לוגואים בעמוד 1 של "דוח סקירה"** — הלוגו שלנו (קבוע) ולוגו מזמין העבודה
  (חדש: שדה העלאה בלשונית "כללי", ליד "שם המזמין") משני צידי כותרת "דו"ח
  סקירה", רק בעמוד הראשון — [index.html](index.html), [app.js](app.js), [pdf.js](pdf.js), [style.css](style.css)
- **תוכן פגמים בתקציר המנהלים** — לכל פגם משמעותי ברכיב הקריטי, פסקת
  הנחיה כללית (סיבה אפשרית / השלכה אם לא יטופל / כיוון תיקון), מתוך קטלוג
  חדש המכסה את כל 137 קודי הפגם — [defect_guidance.js](defect_guidance.js), [render.js](render.js)
- **תקציר המנהלים מעוצב רשמי יותר** — הוסרו אימוג'ים, כותרות פורמליות,
  ותוקן בלבול bidi בכותרות המעורבות עברית/אנגלית (CPIav/CPIcrit)
  — [render.js](render.js)
- **QR מרוכז בעמוד אחד** — כל קודי השחזור יחד ברשת אחת עם כותרת ברורה לכל
  קוד ("חלק N מתוך M — ללא תמונות"), במקום 2 קודים לעמוד — [pdf.js](pdf.js), [style.css](style.css)
- **כפתור "🧮 ייצוא חישוב ציון"** — PDF נפרד ונקי עם כל הנוסחאות וההצבות
  בפועל לפי הנוהל (ECS/ECF/ECI/EIF/SCS/CPI), תוכן זהה ללשונית "בקרה" אך
  מיועד להדפסה — [index.html](index.html), [app.js](app.js), [pdf.js](pdf.js), [render.js](render.js), [style.css](style.css)
- **מסך קוד גישה** — חוסם כניסה לאתר עד הזנת קוד (נשמר בדפדפן לביקורים
  הבאים). הגנה בסיסית בלבד (קוד מוצפן ב-SHA-256 בצד הלקוח, לא אימות
  אמיתי) — [index.html](index.html), [style.css](style.css)

### Fixed (תוקן)
- **בזבוז שטח משמעותי בעמודי טבלאות ארוכות (בעיקר "תיעוד סקירת רכיבים")**
  — מדידת גובה השורות לפיצול-לעמודים בוצעה ב-`<div>` בלי מחלקת
  gov-page/idcard-page, כך שירשה גופן ברירת מחדל של הדפדפן (16px) במקום
  הגופן האמיתי בדוח (13.3px/11px) — הערכת-יתר של כ-15% בגובה כל שורה
  שגרמה לעצירה מוקדמת מדי. בבדיקה על BR-11: 18→23 שורות בעמוד, ומספר
  העמודים הכולל של הדוח ירד מ-17 ל-14 — [pdf.js](pdf.js)
- **בלבול מילים (bidi) במקרא S/Ex/Def** בתחתית "תיעוד סקירת רכיבים" —
  מונחים באנגלית עטופים כעת ב-`<bdi>` כדי שלא יתערבבו עם הטקסט העברי
  הסמוך — [pdf.js](pdf.js)

## [1.19.1] — 2026-09-15

### Fixed (תוקן)
- **הערות הסוקר לא הופקו ב-PDF בכלל** — במקטע 5 של "דוח סקירה" ("הערות
  הסוקר") הוצג רק טקסט אוטומטי (פרשנות ציוני CPI); ההערות החופשיות
  שהוזנו בפועל בלשונית "הערות הסוקר" (`state.surveyorNotes`) נשמרו
  בנתונים (ZIP/QR) אך מעולם לא נקראו משם — כפילות השם בין הלשונית לבין
  מקטע הדוח יצרה רושם מטעה שהן כן מודפסות. עכשיו הן מתווספות כשורות
  נוספות באותו מקטע, עם פיצול-עמודים אוטומטי כמו בשאר טבלאות הדוח —
  [pdf.js](pdf.js)
- **שני תאריכי הסקירה (נוכחית/הבאה) בעמוד 1 של הדוח, ותאריכי הסקירה
  הקודמת/נוכחית בייצוא ת.ז, לא היו באותו פורמט** — חלקם הוצגו כמחרוזת
  ISO גולמית (`2026-09-14`) וחלקם לפי `toLocaleDateString`. כל התאריכים
  מוצגים עכשיו באופן אחיד כ-`DD/MM/YYYY` — [pdf.js](pdf.js), [render.js](render.js)

## [1.19.0] — 2026-09-14

### Added (נוסף)
- **הוספת שורה במיקום מדויק בלשונית "ממצאים"** — לחיצה על מספר השורה מוסיפה
  שורה ריקה מעליה (השורות שאחריה נדחות למטה), בלי צורך תמיד לרדת לסוף
  הרשימה. כפתור "➕ הוסף שורת תיעוד" הקיים בתחתית ממשיך לעבוד כרגיל —
  [app.js](app.js), [render.js](render.js), [style.css](style.css)
- **עריכת פגם קיים ב"סקירת המבנה"** — כפתור עיפרון (✏️) ליד כל פגם פותח
  מחדש את טופס הפגם עם כל הנתונים הקיימים למילוי/תיקון, גם אחרי שהפגם
  כבר נשמר. "💾 שמור שינויים" מעדכן את הרשומה הקיימת במקום ליצור כפולה —
  [app.js](app.js), [render.js](render.js), [style.css](style.css)
- **התנהגות Enter אחידה בכל שדות הטקסט בתוכנה** — מעבר לשדה הבא עם בחירה
  מלאה (select) של התוכן הקיים בו, כך שהקלדה מיידית מחליפה אותו בלי מחיקה
  ידנית. בעבר זה עבד רק בטבלאות שורה-אחר-שורה; עכשיו זה חל גם על רשתות
  שדות (ת.ז, כללי, מהנדס/סוקר) וטופס הוספת/עריכת הפגם — [app.js](app.js)

### Fixed (תוקן)
- **סדר רכיבים/פגמים/תמונות בדוח לא היה עקבי עם המספור** — הרכיבים
  והפגמים הוצגו בסדר ההזנה למערכת במקום לפי מספר הקטלוג/קוד הפגם. נוסף
  מיון היררכי-מספרי (לא טקסטואלי — "9" לפני "12", לא אחריו) שמוחל בכל
  מקום שבו הדוח מרכיב רשימת רכיבים/פגמים: טבלת תיעוד סקירת הרכיבים, סיכום
  הכמויות/ECS, וייצוא ת.ז. גם גלריית התמונות בסוף הדוח בנויה עכשיו מתוך
  אותה רשימה ממוינת, כך שסדר התמונות תואם את סדר הופעתן בגוף הדוח —
  כולל תיקון לכך שהלולאה איספה תמונות ממקטע "רכיבים" לפני מקטע "ממצאים",
  הפוך מסדר המקטעים בפועל בדוח — [pdf.js](pdf.js)
- **Enter בטופס הוספת/עריכת פגם לא עשה כלום** — הטופס יושב בפועל בתוך תא
  colspan של טבלת הסקירה, כך ש-Enter "נפל" לחיפוש שורה הבאה בטבלה (ואין
  כזו אחרי שורת הפעולה האחרונה) במקום לשדה הבא בטופס עצמו — [app.js](app.js)

## [1.18.1] — 2026-09-14

### Fixed (תוקן)
- **התיקון הקודם לחפיפה ב"סקירת המבנה" לא כיסה את המקרה האמיתי** — הבעיה לא הייתה
  ברשימת הרכיבים עצמה אלא בטבלת הפגמים שלצידה: `table-layout:auto` (ברירת המחדל)
  מתייחס ל-`width:100%` כמינימום בלבד, לא כמקסימום. כשטופס הוספת פגם נפתח בתוך
  תא ה-colspan, הוא "דרש" יותר רוחב מכפי שהוקצה, והטבלה כולה גדלה מעבר לגבול
  התיבה שלה — ב-RTL ההתרחבות יצאה שמאלה, ישר לתוך רשימת הרכיבים. נוסף
  `table-layout: fixed` עם רוחבי עמודות מפורשים על שורת הכותרת
  — [render.js](render.js), [style.css](style.css)
- **אותה בעיה בדיוק, שכבה עמוקה יותר**: בתוך טופס הפגם עצמו, `.grid-2` (רשת
  שדות) משתמש ב-`1fr 1fr` — ברירת המחדל של רצועת גריד כזו היא `minmax(auto,1fr)`,
  לא `minmax(0,1fr)`, כך שרשימה נפתחת (כמו "תת-רכיב") עם טקסט אפשרויות ארוך
  לא מתכווצת מתחת לרוחב התוכן שלה ודוחפת את הרצועה עצמה לרוחב — וה-`max-width:100%`
  שכבר היה על ה-select לא עוזר, כי הוא מגביל רק בתוך הרצועה שכבר התרחבה. נוסף
  `min-width: 0` לילדי `.grid-2` — [style.css](style.css)

### Added (נוסף)
- **קובץ התרשימים (DWG) מצורף בפועל ל-📦 ייצוא סקירה (ZIP)**, לא רק שמו כטקסט —
  כך מנהל האחזקה מקבל את השרטוט עצמו יחד עם הדוח בקובץ אחד, בלי צורך להעלות אותו
  בנפרד למערכת אחרת. חל רק כשהקובץ **נבחר בפועל** דרך "📎 בחר קובץ DWG" (יש Blob
  בזיכרון) — הקלדה ידנית של שם קובץ ממשיכה לא לצרף כלום, כמו קודם. טעינה חוזרת
  של אותו ZIP משחזרת גם את קובץ ה-DWG (זהה בייט-לבייט) יחד עם הטקסט והתמונות
  — [app.js](app.js), [pdf.js](pdf.js), [index.html](index.html)

## [1.17.0] — 2026-09-14

### Added (נוסף)
- **תמונת חזית המבנה ניתנת להזנה גם מלשונית "כללי"**, בלי למלא ת.ז בכלל — לא כל
  מבנה מחייב ת.ז, אבל תמונת החזית בעמוד 1 של הדוח (כמו בתוצר הרשמי של נתיבי
  ישראל) רלוונטית תמיד. זה אותו שדה בדיוק כמו "קוד תמונה ראשית" בלשונית ת.ז
  (`state.idCardMainPhoto`) — לא שדה כפול; מילוי באחת הלשוניות משתקף מיד בשנייה
  — [index.html](index.html), [app.js](app.js)

## [1.16.1] — 2026-09-14

### Fixed (תוקן)
- **רשימת הרכיבים ב"סקירת המבנה" יכלה לגלוש מעל טבלת הפגמים** — הרשימה חסמה
  גלישה אנכית בלבד (`overflow-y`), לא אופקית. כל תוכן שיצא רחב מ-300px (תווית
  ארוכה, הבדל גופן/זום בין דפדפנים) לא נחסם בגבול הרשימה והמשיך לצייר ימינה,
  מעל תוכן הרכיב הנבחר. נוסף `overflow-x: hidden` וגם `min-width: 0` על תא השם
  (חסר קודם — בלעדיו ילד flex לא מתכווץ מתחת לרוחב התוכן הפנימי, ואז
  `text-overflow: ellipsis` לא נכנס לפעולה) — [style.css](style.css)

## [1.16.0] — 2026-09-09

### Changed (שונה)
- **"סקירת המבנה" חזרה למבנה master-detail**: רשימת כל הרכיבים (מקובצת לפי מפתח)
  בצד אחד, וטבלת הפגמים של **הרכיב הנבחר בלבד** בצד השני — כדי לרשום פגמים לרכיב
  אחד בלי לראות/לגלול בין כל פגמי הגשר. לחיצה על רכיב אחר ברשימה מחליפה את התוכן
  המוצג; מחיקת הרכיב הנבחר עוברת אוטומטית לרכיב אחר קיים. "רכיבים" (הצהרת כמות
  ומדידות) נשארה רשימה פתוחה כרגיל — השינוי נוגע רק למסך הפגמים
  — [app.js](app.js), [render.js](render.js), [style.css](style.css)
## [1.15.0] — 2026-09-08

### Changed (שונה)
- **עיצוב "דוח סקירה" הותאם ל-Bridge Inspections.pdf (הדוח הרשמי של נתיבי ישראל)** —
  נבדק ישירות מול הקובץ (גופנים/צבעים/גבולות בפועל, לא רק מראה חזותי): Arial 10pt
  בעמוד, גבולות שחורים דקים במקום אפור בינוני, ובלי הדגשה/רקע אפור על תוויות טבלה
  שבמקור מוצגות כטקסט רגיל לבן (התוויות בטבלת הכותרת החוזרת, ותוויות רשימת הנתונים
  הכלליים) — הדגשה נשארת רק היכן שהמקור מדגיש בפועל (כותרות עמודה, ערכי CPI)
  — [style.css](style.css), [pdf.js](pdf.js)
- **"נתונים כלליים" (עמוד 1) נבנה מחדש כרשימת שורה-אחת-לשדה** (לא שני שדות זה-לצד-זה),
  עם קודי הסעיפים מהנוהל (01.01, 01.02 וכו') לצד כל תווית — תואם את הגרנולריות של
  העמוד המקביל במקור, ועקבי עם הקידוד שכבר קיים בלשונית ת.ז. תמונת "צילום ראשי"
  (אם הוגדרה בת.ז) מוצגת לצד הרשימה, בדיוק כמו בעמוד המקביל במקור — [pdf.js](pdf.js)
- **"דו"ח סקירה" — כותרת מודגשת ומרוכזת בעמוד הראשון בלבד**, מעל הטבלה החוזרת,
  כמו במקור (בשאר העמודים אין אותה) — [pdf.js](pdf.js)
- **כותרת סעיף 1 בלי מספר סוגריים** ("נתונים כלליים:" ולא "[1] נתונים כלליים") —
  כך גם במקור; שאר הסעיפים ("[2] תיעוד ממצאים" וכו') ללא שינוי — [pdf.js](pdf.js)
- **נוסף מקרא S/Ex/Def.** מתחת לטבלת "תיעוד סקירת רכיבים" (בעמוד האחרון של המקטע
  בלבד), כמו במקור — [pdf.js](pdf.js)

## [1.14.0] — 2026-09-07

### Fixed (תוקן)
- **איכות התרשימים בדוח הייתה ירודה** — משתי סיבות שהצטברו: (1) תרשים נשמר באותה
  הקטנה כמו תמונה (1200px, איכות 0.8), אף שהוא תופס עמוד שלם ומורכב מקווים דקים
  וטקסט — כ-118dpi בגודל ההדפסה בפועל; (2) הוא עבר רסטריזציה לרזולוציית העמוד
  ואז נדחס כ-JPEG בשנית. כעת תרשים נשמר עד 3000px באיכות 0.92 (כ-295dpi) ומוטמע
  ישירות ב-PDF ברזולוציה המקורית במקום לעבור דרך רסטר העמוד — דחיסה אחת במקום
  שתיים. תרשים שרזולוציית המקור שלו נמוכה יותר נשמר כמות שהוא (אין הגדלה), ותמונות
  רגילות לא השתנו — [app.js](app.js), [pdf.js](pdf.js)
- **תמונות בדוח יצאו מתוחות ("מרוחות")** — `object-fit: contain` שבו הסתמכנו אינו נתמך
  ב-html2canvas, שמצייר כל תמונה לתוך תיבת ה-`<img>` ומותח אותה. כעת מחושב לכל תמונה
  הגודל שמשמר את יחס הצדדים בתוך התיבה שהוקצתה לה. חל על תמונות הדוח (4 בעמוד),
  על התרשימים (אחד בעמוד) **וגם על קודי ה-QR** — שנמתחו אף הם, מה שפוגע באמינות
  הסריקה מדף מודפס — [pdf.js](pdf.js)
- **רקע ירוק/אפור מסביב למספרי התמונות** בעמוד הממצאים: תגיות "צורף/לא צורף" הן חיווי
  מסך בלבד וחסרות משמעות בדוח מודפס. כעת הקודים מוצגים כטקסט רגיל — [pdf.js](pdf.js)
- **טבלת "נתונים כלליים" בעמוד 1 לא הייתה מיושרת** — פריסת טבלה אוטומטית נתנה לכל שורה
  רוחב עמודות משלה לפי אורך התוכן. נקבע `table-layout: fixed` עם רוחבי עמודות
  אחידים — [style.css](style.css)

### Added (נוסף)
- **חבילת ה-ZIP כוללת כעת גם את כל התמונות**, ו-**📂 טען מקובץ** יודע לטעון את ה-ZIP
  עצמו ולשחזר את הסקירה **במלואה — כולל התמונות**. כך אפשר לתקן פרט קטן בסקירה שכבר
  הופקה ולהפיק את הדוח מחדש בלי לצרף שוב את תיקיית התמונות. התמונות נשמרות בתוך ה-ZIP
  כקבצים בינאריים ולא כ-base64 בתוך ה-JSON, שהיה מנפח אותן ב-33% ללא תועלת. קובץ
  הטעינה JSON נשאר כשהיה (טקסט בלבד) וממשיך להיטען כרגיל
  — [pdf.js](pdf.js), [app.js](app.js), [index.html](index.html)

## [1.13.0] — 2026-09-07

### Added (נוסף)
- **Enter יורד לשורה הבאה** באותה עמודה בטבלאות ממצאים, סקיצות והערות — מילוי רציף
  כמו בגיליון אלקטרוני, במקום Tab שעובר לשדה הבא באותה שורה. בשורה האחרונה הפוקוס
  נשאר במקומו, ובקומבו הפגמים Enter ממשיך לבחור פגם כרגיל — [app.js](app.js)

### Fixed (תוקן)
- **הפוקוס קפץ לשורה העליונה** אחרי הזנת ערך בטבלאות שורה-אחר-שורה: `captureFocus`
  בנה סלקטור עם `data-sub`/`data-span` בלבד, ולכן שורות שמזוהות ב-`data-finding`/
  `data-sketch`/`data-note`/`data-code` קיבלו סלקטור זהה והפוקוס הוחזר לראשונה שנמצאה.
  כעת כל מזהה `data-*` נכלל בסלקטור — התיקון חל גם על הממצאים, הסקיצות, ההערות
  ושדות ת.ז — [app.js](app.js)
- **קודי הפגמים תוקנו לפורמט הרשמי `FF.DD`** — שני החלקים בני שתי ספרות, כפי שהם
  מופיעים בדוח הסקירה הרשמי של נתיבי ישראל: פגיעה מכאנית היא **14.01** ולא 14.1,
  סדקים בבטון הם **02.04** ולא 2.4. 104 מתוך 137 הקודים בקטלוג עודכנו
  — [defects.js](defects.js)
- **סקירות שמורות ממשיכות לעבוד**: קוד בפורמט הישן מתורגם אוטומטית בטעינה (מהאחסון
  המקומי, מקובץ טעינה ומקוד QR) — בלעדיו שם הפגם היה נעלם מהטבלאות ומהדוח,
  כי הקוד לא היה נמצא בקטלוג — [app.js](app.js)
- **שחזור מ-QR עבר להשתמש במיגרציה** כמו כל מסלול טעינה אחר. עד כה הוא דילג עליה
  לגמרי, כך שקוד שהודפס בגרסה ישנה לא קיבל אף אחת מההתאמות — [app.js](app.js)
- חיפוש בקומבו הפגמים מזהה גם את הכתיב הישן: הקלדת "14.1" מוצאת את 14.01, בלי
  שהקלדת "01.14" תיתפס בטעות — [combobox.js](combobox.js)

## [1.12.0] — 2026-09-07

### Changed (שונה)
- **37 שדות בת.ז הפכו לשדות מספריים עם עיגול אוטומטי לפי הנוהל** — כל סעיף מקבל את
  רזולוציית המדידה וכיוון העיגול שמוגדרים לו ב"מדריך לתיעוד גשרים, מנהרות ומבני דרך"
  (מהדורה 5-2019). העיגול מתבצע ביציאה מהשדה, וההנחיה מוצגת מתחתיו
  — [data.js](data.js), [app.js](app.js), [render.js](render.js)
  - **מידות (סעיף 4):** אורכים ורוחבים 10 ס"מ כלפי מעלה · מדרכות/הגבהות 5 ס"מ כלפי
    מעלה · **מרווחים חופשיים (4.18–4.20, 4.22) ורוחבי מסלול (4.14–4.15) — כלפי מטה**,
    כדי שלא יוצהר על מרווח גדול מהקיים בפועל · Skew (4.17) כמספר שלם ·
    שילוט מגבלת גובה (4.21) מועתק מהשלט ואינו מעוגל
  - **שנים ומניינים:** שנת בנייה/שיקום/מדידת AADT, מספר מסלולים ונתיבים (מעל ומתחת),
    מספר סוגי מבנה עליון ונציבי ביניים, ק"מ התחלה — מספרים שלמים
  - **תנועה:** AADT כמספר רכבים · **3.15 תוקן לאחוז משאיות** (לפי המדריך זהו אחוז
    מהתנועה היומית ולא מספר רכבים) · אורך מעקף (3.17) מעוגל כלפי מעלה לק"מ שלם
  - **הידראוליקה ומדדים:** מפלס יחסי (9.1) 10 ס"מ כלפי מעלה · תקופת חזרה (9.2) בשנים ·
    Availability/Reliability PI (11.1/12.1) בטווח 0–100
- **ניקוי ערכי טקסט חופשי משדות שהפכו למספריים** — מצב שמור שהכיל טקסט בשדה מספרי
  (למשל "כ-4.5 מ'") מנוקה בטעינה; מפרידי אלפים נשמרים כמספר ("98,000" ⇒ 98000)
  — [app.js](app.js)

## [1.11.0] — 2026-09-06

### Changed (שונה)
- **לשונית "רכיבים" פוצלה לזרימת עבודה בסגנון גיליון עבודה**, בשני תת-טאבים חדשים: "סיכום
  רכיבים" (הצהרה על כל רכיב וכמותו, ושיוך למפתח — טבלה שטוחה אחת לכל המפתחים) ו"פירוט
  הרכיבים" (מילוי מידה/מידה משנית לכל רכיב, רשימה פתוחה). ניווט המפתחות/רכיבים בסגנון
  master-detail (טאבים למפתחות + רשימת-אב/פירוט) הוסר — כל הרכיבים גלויים בבת אחת בכל
  תצוגה — [index.html](index.html), [app.js](app.js), [render.js](render.js), [style.css](style.css)
- **לשונית חדשה "סקירת המבנה"** (לצד "רכיבים") — טבלה אחת לכל מפתח (כותרת "מפתח X" בלבד,
  כדי לצמצם גלילה) עם כל הרכיבים שלו יחד: עמודת "רכיב" (בשם הרכיב, לא בקוד הפגם) מקבצת
  את כל שורות הרכיב, ולידה תיבת "נסקר", כפתור "✔️ רכיב תקין" וטבלת הפגמים (הועברו מהכרטיס
  המשולב הישן). ברירת המחדל "רכיב תקין = 1A" ללא רשומות פגם לא השתנתה — רק מיקום/עיצוב
  הבקרות ב-UI — [index.html](index.html), [app.js](app.js), [render.js](render.js), [style.css](style.css)
- שינוי סוג רכיב קיים מהקטלוג (בטבלת "סיכום רכיבים") ושיוך רכיב קיים למפתח אחר נתמכים כעת
  ישירות בטבלה — קודם ניתן היה רק להוסיף רכיב חדש. מבנה הנתונים (`state.spans[i].components`)
  לא השתנה — `calc.js`, `pdf.js`, `recovery.js` (QR) וה-ZIP export/import ממשיכים לעבוד ללא שינוי

## [1.10.0] — 2026-09-02

### Changed (שונה)
- **קואורדינטות פוצלו ל-X/Y** בלשונית "כללי", כדי להתאים בדיוק לשני השדות הנפרדים בת.ז
  (1.10/1.11). מצב שמור וקוד QR מהגרסה הקודמת (עם השדה המשולב) מפוצלים אוטומטית בטעינה.
- **ארבעה שדות בת.ז נמשכים אוטומטית** מלשונית "כללי" במקום להיות ערוכים בנפרד: 1.6 כביש
  (מ-"כביש מס'"), 1.10/1.11 קואורדינטה Y/X, ו-3.21 מתכנן מקורי (מ-"מתכנן המבנה"). 3.22
  "מתכנן שיקום/הרחבה" נשאר שדה עצמאי בת.ז — [data.js](data.js), [render.js](render.js), [pdf.js](pdf.js)

## [1.9.0] — 2026-09-02

### Added (נוסף)
- **חמישה שדות חדשים** ב"פרטי המבנה" (לשונית כללי): שם המזמין (הנחיה: "עירית ירושלים"),
  סוג הסקירה (ברירת מחדל "שגרתית", ניתן לעריכה), מתכנן המבנה, קואורדינטות (הנחיה:
  "184634 668116"), כביש מס'. נשמרים, משוחזרים ב-QR ובקובץ הטעינה, ומודפסים בעמוד
  הראשון של דוח הסקירה — [index.html](index.html), [app.js](app.js), [recovery.js](recovery.js), [pdf.js](pdf.js)

## [1.8.0] — 2026-09-02

### Added (נוסף)
- **📦 ייצוא סקירה (ZIP)** — קובץ אחד שמכיל גם את דוח הסקירה (PDF) וגם קובץ טעינה (JSON) עם כל
  הנתונים שהוזנו (ללא תמונות). מיועד להעברה/שיתוף/גיבוי כקובץ בודד — [pdf.js](pdf.js)
- **📂 טען מקובץ** — טוען סקירה בחזרה מקובץ ה-JSON שבתוך ה-ZIP, בדיוק כמו שחזור מ-QR (אותה
  אזהרת אישור לפני החלפת הנתונים, ואותה הבהרה שהתמונות אינן כלולות) — [app.js](app.js)
- לשונית "הסבר" מתועדת עם שתי היכולות החדשות (מקטעים 7 ו-9)
- וונדור מקומי חדש: [vendor/jszip.min.js](vendor/jszip.min.js) (JSZip 3.10.1)

### Changed (שונה)
- לוגיקת בניית ה-PDF של "דוח סקירה" חולצה לפונקציה משותפת (`buildReportPdf`) המשמשת גם
  את ההורדה הישירה וגם את ה-ZIP — ללא שינוי בהתנהגות הקיימת — [pdf.js](pdf.js)

## [1.7.0] — 2026-09-02

### Added (נוסף)
- **לשונית "הסבר"** — מדריך שימוש מלא בתוך המערכת, בתשע מקטעים: סדר העבודה המומלץ ·
  מבנה תיקיית התמונות במחשב (עם עץ תיקיות לדוגמה) · איך מקשרים תמונה לשורה לפי שם הקובץ ·
  איך מעלים את הקבצים · מה יש בכל לשונית · נקודות חשובות ברכיבים ופגמים · ייצוא הדוחות ·
  שחזור סקירה מקוד QR · שמירה, איפוס וגיבוי. כולל הבהרה מודגשת שהתמונות אינן נשמרות בין
  טעינות ויש לצרפן מחדש לפני הייצוא — [index.html](index.html)

## [1.6.1] — 2026-09-02

### Fixed (תוקן)
- **אובדן נתונים בכל ארבע טבלאות ההערות** (שינויים · סוקר · מהנדס · תקשורת): הערה שהוקלדה
  זה עתה נמחקה בלחיצה על "הוסף הערה", אם אירוע ה-change לא הספיק להירשם לפני הלחיצה.
  זהו בדיוק הבאג שתוקן קודם בממצאים ובסקיצות אך לא נסרק בשאר הרשימות. שלושת המקומות
  אוחדו לפונקציה אחת (`flushRowInputs`) כדי שלא ייפרד שוב, והפוקוס עובר עכשיו לשורה
  החדשה גם בהערות — [app.js](app.js)
- **"מידה משנית" (size2) נעלמה בשחזור מקוד QR** — השדה נוסף ללשונית "רכיבים" אך לא נכלל
  בקידוד, כך שסוקר ששחזר דוח קיבל את כל התוכן פרט למידה הזו, בלי שום התראה. נוסף לקידוד
  ולפענוח, בשמירה על תאימות לאחור: קוד שהודפס לפני התיקון עדיין נטען, פשוט בלי השדה —
  [recovery.js](recovery.js)
- תגית "✔ קובץ הועלה" של קובץ ה-DWG שרדה איפוס, טעינת דוגמה ושחזור מ-QR — והצהירה על
  קובץ שכבר אינו קיים; מתאפסת עכשיו בשלושת המסלולים — [app.js](app.js)
- "איפוס כל הנתונים" מנתק גם את הקבצים המצורפים בסשן — אחרת תמונות של המבנה הקודם המשיכו
  להופיע כ"צורף" עבור מבנה חדש; ומאפס גם את הלשונית המשנית הפעילה ב"ת.ז"
- טעינת דוגמת BR-11 יצרה תתי-רכיבים ללא השדה `size2` (חוסר עקביות במבנה הנתונים)

## [1.6.0] — 2026-09-02

### Added (נוסף)
- **מס"ד אוטומטי** בלשוניות "ממצאים" ו"סוקר" — עמודה ראשונה עם מספר סידורי רץ מ-1, מתעדכן
  אוטומטית בהוספה/הסרה של שורה. עמודת "ממצאים" שונתה ל-מס"ד / תיאור הממצאים / שם התמונה
  (הסטטוס עבר להיות מוצג בתוך עמודת שם התמונה) — [render.js](render.js)
- **"כמות רכיבים במפתח זה"** בלשונית "רכיבים" — קלט כמות מייצר מיד את מספר הנכון של שורות
  תת-רכיב למילוי המידה, במקום להוסיף שורות אחת-אחת; לרכיבים עם מידה משנית (לפי המתודולוגיה,
  למשל "קורה ראשית") נוספה עמודה ייעודית לה. הפוקוס אחרי בחירת רכיב מהקטלוג עובר עכשיו
  לשדה הכמות, לא למידה — [app.js](app.js), [render.js](render.js)
- **"הוספה למפתח"** בלשונית "רכיבים" — תיבת בחירה מפורשת ליד קטלוג הרכיבים, כדי שאפשר יהיה
  לבחור לאיזה מפתח משייכים רכיב חדש בלי להזדקק להבין שהטאב הפעיל קובע; מסונכרנת דו-כיוונית
  עם טאבי המפתחות — [index.html](index.html), [app.js](app.js)
- **"רכיב תקין" הוא ברירת המחדל** לכל רכיב חדש (לפי הנוהל: רכיב ללא פגם רשום = תקין), במקום
  להתחיל מטבלת פגמים ריקה; נרשם פגם אמיתי מסיר את רשומת ברירת המחדל אוטומטית — [app.js](app.js)
- **"רכיבים קיימים שלא נסקרו"** מוצג עכשיו בשלוש לשוניות (תוצאות, תקציר מנהלים, בקרה) —
  המנוע החישובי כבר החריג אותם נכון קודם, אך זה לא היה גלוי בשום מקום מלבד תוך כדי עריכת
  הרכיב — [render.js](render.js)
- **רשימת תמונות נדרשות** בלשונית "ממצאים" — כברירת מחדל מתמלאת אוטומטית לפי סיווג המבנה,
  לפי טבלת "ממצאים" במסמכי "דפי עזר לסקירה" של נתיבי ישראל (גשר רכב/מובל/גשר שילוט/קיר
  תומך — 16–21 שורות בהתאם לסוג); שינוי סיווג מחליף את הרשימה רק אם היא עדיין לא נערכה —
  [data.js](data.js), [app.js](app.js)
- **"קובץ התרשימים"** בלשונית "תרשימים" — שדה טקסט + כפתור בחירת קובץ DWG אמיתי מהמחשב,
  עם תגית ✔ המאשרת שקובץ נבחר בפועל (session בלבד, לא נשמר — הקובץ עצמו אינו נכלל בדוח,
  פורמט CAD שלא ניתן להטמעה כתמונה) — [index.html](index.html), [app.js](app.js)
- **סעיף 2.12** בלשונית "ת.ז" — שדה עם הנחיה "ימולא ע"י מנהל תחום סקירת גשרים"; תשתית
  כללית (`placeholder`) לסימון שדות המיועדים למילוי גורם אחר מהסוקר — [data.js](data.js)

### Changed (שונה)
- שם הסוקר ושם חברת הסקירה עברו מלשונית "סוקר" ללשונית "כללי"; "סוקר" מכילה עכשיו רק את
  טבלת הערות הסוקר — [index.html](index.html), [render.js](render.js)
- הוסרו האייקונים (🏗️/🎯) מכותרות "זיהוי המבנה" ו"הציונים ומשמעותם" בתקציר המנהלים
- ההנחיה בתוך שדה "קוד תמונה ראשית" בת.ז שונתה ל"תמונת חזית המבנה" ומיושרת לימין

### Fixed (תוקן)
- לחיצה על "הוסף שורת תיעוד"/"הוסף סקיצה" הייתה עלולה לאבד את מה שהוקלד זה עתה בשורה
  הקודמת, תלוי בתזמון blur/click בפועל — כעת ההוספה קוראת ישירות מה-DOM לפני שהיא שומרת,
  ופותחת את השורה החדשה עם פוקוס מיידי — [app.js](app.js)
- קוד סקיצה מקבל כברירת מחדל מספור רץ (001, 002, …) לפי הקוד המספרי הגבוה ביותר הקיים,
  ונשאר ניתן לעריכה מלאה — [app.js](app.js)

## [1.5.0] — 2026-09-01

### Added (נוסף)
- **"תשומת לב מיידית"** בלשונית "כללי", מתחת לפרטי המבנה — טקסט חופשי וקוד תמונה לליקוי
  הדורש טיפול או דיווח מיידי. כשמולא, מוצג מודגש במסגרת אדומה בעמוד הראשון של דוח הסקירה
  המודפס (לפני שאר המקטעים), ותמונתו מופיעה ראשונה בנספח התמונות. משוחזר גם דרך קוד ה-QR
  (תואם לאחור לקודים שהודפסו לפני התוספת) — [index.html](index.html), [pdf.js](pdf.js)
- **לשונית "בקרה"** (אחרי "ת.ז") — פירוט מלא ומאומת של שרשרת החישוב לכל רכיב ומפתח:
  S ו-Ext ← ECS (טבלה 11) ← ECF (טבלה 12) ← ECI ← EIF (טבלה 13) ← שקלול ל-SCS ← המרה
  ל-Condition PI, עם נוסחה סימבולית ולצידה הצבת הנתונים בפועל לכל שלב — כדי לאפשר אימות
  ידני של הציון. מסך בלבד ואינו מודפס במלואו: הדוח המודפס כבר כולל את מקטע [4] (סיכום
  כמויות וציוני ECS), שהוא העמוד המקביל בדוח הרשמי של נתיבי ישראל — [render.js](render.js)

### Changed (שונה)
- הוסרה ההערה "מבנה בעל מפתח אחד או שניים נסקר ומחושב כיחידה אחת..." שהוצגה בלשונית
  "כללי" למבנים בני מפתח אחד/שניים — [app.js](app.js)

## [1.4.0] — 2026-09-01

### Added (נוסף)
- **צירוף תמונות וסקיצות לפי קוד** — בכל שורת פגם נרשם קוד תמונה (או כמה, מופרדים ב-`;`),
  וכפתור "הוסף קבצים מצורפים" שולף את הקבצים המתאימים מתיקיות `תמונות`/`סקיצות` של הגשר.
  תגית ירוקה/אפורה מציגה לאיזה קוד כבר צורף קובץ, ובסיום מוצג דוח קודים שלא נמצא להם
  קובץ. הקבצים נשמרים ל-session בלבד ולא ל-localStorage (מכסת 5MB) — [app.js](app.js)
- **ייצוא "דוח סקירה" בפורמט הרשמי** — A4 לרוחב, שבעה מקטעים ממוספרים, כותרת המבנה ומספור
  העמוד חוזרים בכל עמוד. הפיצול לעמודים נעשה לפי **מדידת גובה שורות אמיתי** ולא לפי הערכה,
  וכותרת הטבלה חוזרת בראש כל עמוד המשך — [pdf.js](pdf.js)
- **"תקציר מנהלים" כמסמך PDF נפרד** (A4 לאורך) ולא כחלק מהדוח הראשי
- **שחזור נתונים מקוד QR — בלי שרת.** הדוח המודפס נושא נספח עם קוד/י QR שמקודדים את כל
  התוכן שהוזן (דחיסת Deflate + פיצול אוטומטי לכמה קודים כשצריך). סוקר שמגיע לגשר שכבר
  נסקר סורק אותם — מהמצלמה או מתמונה — ומקבל את הטופס המלא בחזרה במקום להקליד מאפס.
  **התמונות אינן נכללות** ויש לצרפן מחדש — [recovery.js](recovery.js)
- **לשונית "ת.ז"** — תעודת זהות לגשר ומובל (מהדורה 6-2008): 104 שדות בשש קבוצות, כאשר
  שדות שכבר הוזנו בלשוניות אחרות (מספר/שם המבנה, מספר מפתחים, Condition PI, תאריכי
  סקירה) נמשכים אוטומטית ומוצגים לקריאה בלבד. ייצוא PDF תואם לטופס הרשמי — [data.js](data.js)

### Changed (שונה)
- **ארגון מחדש לעשר לשוניות בסדר של מערכת ה-BMS**: כללי · שינויים · ממצאים · רכיבים ·
  סוקר · מהנדס · תרשימים · תקשורת — ואחריהן תוצאות ותקציר מנהלים, שאין להן מקבילה
  ב-BMS, ולבסוף ת.ז. התוכן שהיה מרוכז ב"כללי" פוזר ללשוניות הייעודיות — [index.html](index.html)
- כותרת הכלי היא "דו״ח סקירה", ולוגו החברה מופיע בכותרת ובלשונית הדפדפן

### Fixed (תוקן)
- **מזהי רכיבים כפולים אחרי טעינה שנייה — עלול היה למחוק נתונים.** בטעינה מ-localStorage
  מונה המזהים קפץ לערך קבוע (10000) במקום להמשיך מהמזהה הגבוה הקיים: רכיב שנוסף בכניסה
  אחת קיבל `u10000`, ורכיב שנוסף בכניסה הבאה קיבל **את אותו מזהה** — ואז מחיקת אחד מהם
  מחקה גם את השני. המונה ממשיך עכשיו מהמזהה הגבוה שכבר קיים בנתונים — [app.js](app.js)
- לחיצה על "ביטול" בשאלת שחזור ה-QR אִפסה את הסריקה בלבד, ולולאת המצלמה זיהתה מיד את
  אותו קוד והקפיצה את השאלה שוב ללא הפסקה — "ביטול" סוגר עכשיו את הסורק
- כישלון בהפקת קוד ה-QR הפיל את ייצוא הדוח כולו — הנספח הוא תוספת רשות ומדולג בשקט
- קוד ה-QR בהדפסה היה קטן מדי לסריקה אמינה מדף מודפס (0.64 מ״מ למודול): שני קודים לעמוד
  במקום ארבעה מגדילים אותו ל-1.08 מ״מ למודול — [style.css](style.css)
- `img.decode()` יכול היה להיתקע לנצח אם הכרטיסייה עברה לרקע באמצע הייצוא והקפיא אותו —
  הוחלף בהמתנה לאירועי `load`/`error` עם timeout הגנתי
- קודי תמונה מרובים (למשל `4;99`) הוצגו הפוך בהקשר RTL — נוסף `dir="ltr"`
- ציון ה-CPI חושב פעמיים בכל הקלדה מאז הוספת לשונית ת.ז — חישוב אחד משרת את כל הלשוניות
- קוד התמונה הראשית של ת.ז לא נכלל בדוח "קודים שלא נמצא להם קובץ"

## [1.3.1] — 2026-08-31

### Fixed (תוקן)
- הוספת רכיב במקלדת (הקלדת קוד + Tab) העבירה את הפוקוס לשדה השרירותי הבא לפי סדר
  ה-DOM במקום למידת הרכיב החדש: `focusAfter` בקומבו רץ בטיימר מאוחר יותר וגנב בחזרה
  את הפוקוס שהאפליקציה מיקמה בכוונה אחרי הרינדור. עכשיו הוא מוותר אם משהו אחר כבר
  תפס את הפוקוס — [combobox.js](combobox.js)

## [1.3.0] — 2026-08-31

### Changed (שונה)
- **שיפוץ ויזואלי מלא לסגנון מערכת ה-BMS של נתיבי ישראל** ("מנ"ג"), לפי צילומי מסך מהמערכת
  הרשמית — כדי שהניווט יהיה מוכר למי שעובד איתה:
  - פס עליון דחוס (לוגו + כפתורי פעולה) ומתחתיו breadcrumb עם שם ומספר המבנה — [index.html](index.html)
  - **סרגל טאבים אופקי** (כללי · רכיבים ופגמים · תוצאות · תקציר מנהלים) במקום ארבעה
    כרטיסים ממוספרים בגלילה אחת
  - פאנלים במסגרת חדה עם כותרת אפורה וקיפול אמיתי (`<details>`)
  - **רכיבים בפריסת master-detail**: רשימת רכיבים צרה מימין, פאנל פירוט לרכיב הנבחר
    משמאל — במקום כרטיסי רכיב מוערמים במלוא הרוחב — [render.js](render.js)
  - טבלאות צפופות יותר והדגשת שורה נבחרת בכחול, בסגנון הגריד של המערכת — [style.css](style.css)
- טעינת דוגמת BR-11 עוברת לטאב "תוצאות" (במקום גלילה אליו)

### Fixed (תוקן)
- שורות רשימת הרכיבים הן `<button>` ולא `<div>` — הרשימה נגישה ב-Tab ובמקלדת, ולא
  רק בעכבר; מילוי רציף במקלדת הוא עקרון מוביל בכלי הזה — [render.js](render.js)
- פאנל שהמשתמש קיפל הודפס ככותרת ללא תוכן: קיפול `<details>` מיושם דרך
  `::details-content`/`content-visibility` ולא ניתן לביטול ב-CSS — הפאנלים נפתחים
  ב-`beforeprint` ומצבם מוחזר ב-`afterprint` — [app.js](app.js)
- פסי הכותרת נעלמו זה מתחת לזה בגלילה ברוחב צר (היסטי sticky קבועים מול סרגל כלים
  שנשבר לכמה שורות) — שלושת הפסים עברו לקונטיינר sticky אחד — [style.css](style.css)
- הודעת "אין רכיבים במפתח זה" הוצגה פעמיים במסך ריק

## [1.2.1] — 2026-08-26

### Fixed (תוקן)
- **באג חישוב קריטי**: כמה פגמים על אותו תת-רכיב, השווים בחומרה המרבית, סיכמו את ערכי ההיקף
  שלהם והפיקו ECS שאינו קיים בטבלה 11 (למשל 4.4 או אפילו 5.1, מעבר לסולם 1–5) — [calc.js](calc.js).
  לפי סעיף 02.7.4 ("פגמים מרובים ברכיב"), פגמים תואמי-חומרה על אותו תת-רכיב מצטמצמים תחילה
  לזוג (S,Ex) אחד — ההיקף הוא הגרוע מביניהם, לא סכומם. הבאג לא נתפס ב-46 הבדיקות הקיימות כי
  ב-BR-11 בכל התנגשות כזו רק לפגם אחד היה היקף שאינו אפס (A/B=0.0 תמיד); נוספו 5 בדיקות ל-test.js.
- פגם המפנה לתת-רכיב שהוסר (sub לא קיים) עוד לא נספר בטעות במונה בלי להיכלל במכנה — [calc.js](calc.js)
- הקטנת מספר המפתחים מחקה בשקט רכיבים ופגמים ממפתחים שהוסרו — עכשיו נדרש אישור מפורש — [app.js](app.js)
- לחיצה חוזרת על "רכיב תקין" צברה רשומות 1A כפולות — עכשיו מחליפה רשומה יחידה (עם אישור אם יש פגמים אמיתיים) — [app.js](app.js)
- README תיאר שקלול CPI ישיר לצד שיטת הנוהל — התצוגה הוסרה כבר ב-1.2.0; התיאור עודכן — [README.md](README.md)
- הפניה מתה ל-`d.defName` (תמיד undefined) בטבלת הפגמים — [render.js](render.js)

## [1.2.0] — 2026-08-04

### Added (תוסף)
- לוגו "מאיר ויואל" בכותרת האתר ובראש הדוח המודפס — [index.html](index.html)
- שדה הערה חופשית ("איך חושבה המידה") לכל מידת מפתח ותת-רכיב — זכרון ארגוני, לא משפיע על החישוב
- כפתור "שכפל" לכל תת-רכיב — מעתיק מידה, הערה וגם את כל הפגמים המשויכים אליו
- סיווג המבנה לסקירה (טבלת תדירויות 3.4) עבר לפרטי המבנה; מועד הסקירה הבאה מחושב אוטומטית
- תאריך הסקירה הנוכחית מקבל כברירת מחדל את היום, ונשמר עם המבנה
- רכיבים ראשיים/משניים מקבלים את הקוד מטבלה 2/6 (למשל 1.4 קורה ראשית) — גם בחיפוש בקטלוג

### Changed (שונה)
- בחירת רכיב מהקטלוג מוסיפה אותו מיד — הוסר כפתור "הוסף רכיב"
- בחירת פגם בשדה אחד: הקלדת קוד ("14.1") או שם מביאה את הפגם, המשפחה נגזרת אוטומטית
- דרגת החומרה (S) נבחרת ב-select לצד ההיקף (Ex), עם תיאור הפנקס המלא מתחת
- מילוי רציף במקלדת: Tab מאשר בחירה בתיבות הסינון וממשיך לשדה הבא; הפוקוס נשמר בין עדכונים
- תתי-הרכיבים מוצגים פתוחים תמיד — אין צורך ללחוץ כדי לראות פרטים
- תקציר המנהלים עוצב מחדש: שם המבנה בלבד, מדי מהירות צבעוניים (0–100, טווחי טבלה 15) ל-CPIav
  ול-CPIcrit, והרכיב הקריטי בשורה אחת (שם · מפתח · פגם)

### Fixed (תוקן)
- שדה "סוג המנהרה" הוצג גם בגשרים (CSS דרס את מנגנון ההסתרה) — עכשיו מוצג רק במנהרות, ואפשר גם "ללא"
- מחיקת תת-רכיב הסיטה את שיוך הפגמים לתת-רכיב הלא-נכון — השיוך נשמר נכון והפגמים של הנמחק מוסרים
- מידת מפתח 4 בדוגמת BR-11 הוצגה כ-329.79999999999995 — עיגול לשתי ספרות

### Removed (הוסר)
- תצוגת "שיטת הקבצים הקיימים" (שקלול CPI) — נשאר ציון הנוהל בלבד; מנוע החישוב והבדיקות נשמרו
- מהתקציר: "הרכיבים במצב הגרוע ביותר", "התפלגות מצב הרכיבים", "דגלים והערות"
- קובצי האקסל אינם מתוחזקים עוד — ההשקעה באתר בלבד (החלטת 2026-08-04)

## [1.1.0] — 2026-08-04

### Added (תוסף)
- תמיכה במבנים עד 30 מפתחים (במקום 12) — באתר ובקובץ האקסל
- תיבות בחירה עם סינון תוך כדי הקלדה (רכיבים, פגמים, משפחות, סוגי מבנה) — [combobox.js](combobox.js)
- סינון בהקלדה גם באקסל: הרשימות הנפתחות מצטמצמות לפי הטקסט שהוקלד (עובד בכל גרסת Excel)
- ייצוא דוח PDF מלא מהאתר — תוצאות, תקציר מנהלים ונספח פירוט לכל מפתח — [pdf.js](pdf.js)
- באקסל: חסימת ערכים שאינם מהקטלוג עם הודעת שגיאה בעברית

### Fixed (תוקן)
- קריסת התקציר במבנה מרובה-מפתחים לפני הזנת מימדי שקלול — [render.js](render.js)

### Removed (הוסר)
- כפתורי ייצוא/ייבוא JSON — הייצוא היחיד הוא PDF והדפסה; השמירה האוטומטית בדפדפן נשארת

## [1.0.0] — 2026-08-04

### Added (תוסף)
- מחשבון Condition PI מלא לפי נוהל נתיבי ישראל 9-2019 — [calc.js](calc.js)
- קטלוג 137 פגמים מהפנקס לסוקר עם תיאורי דרגות חומרה לבחירה בטופס — [defects.js](defects.js)
- קטלוגי רכיבים לכל 5 סוגי המבנים + 14 סוגי מבנה-על — [data.js](data.js)
- ציון לכל מפתח + ציון מבנה בשתי שיטות שקלול (נוהל 6.2 ושקלול CPI)
- פירוש הציונים לפי טבלה 15 (ממוצע + קריטי) ותקציר מנהלים אוטומטי
- מחשבון מועד סקירה הבאה לפי טבלת התדירויות הרשמית
- שמירה אוטומטית בדפדפן, ייצוא/ייבוא JSON, הדפסת דוח, דוגמת BR-11
- 43 בדיקות אימות מול קבצי BR-11 המקוריים — [test.js](test.js)
